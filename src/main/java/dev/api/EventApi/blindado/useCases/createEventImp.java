package dev.api.EventApi.blindado.useCases;

import dev.api.EventApi.blindado.entities.Event;

public class createEventImp implements IcreateEventUseCase {

    @Override
    public Event execute(Event event){
        return (event);
    }
}
