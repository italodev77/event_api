spring:
  apllication:
    name: eventapi
flyway:
  enabled: false

  datasource:
    url: jdbc:postgresql://localhost:5432/eventDB
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver

  jpa:
    show-sql: true
